server:  port: 8083

spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true    hibernate:      ddl-auto: update
  datasource:    url: jdbc:mysql://localhost:3306/movie_catalog
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver  application:    name: rating-data-service